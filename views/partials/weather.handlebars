<div class="weatherWidget">
        <div class="location">
            <h3>{{name}}</h3>
            <a href="{{forecastUrl}}">
                <img src="{{iconUrl}}" alt="{{weather}}"/>
                <p>{{weather}}, <strong>{{temp}}</strong></p>
            </a>
        </div>
    <small>Source: <a href="http://www.wunderground.com">Weather Underground</a></small>
</div>